#= require spec_helper

describe 'Settings', ->

  it 'works if local storage is not supports by browser'

  it 'should read settings from localStorage'

  it 'should immediately write settings to localStorage'
